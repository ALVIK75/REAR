# Administrator Guide: Backup & Recovery with REAR Automation

Этот документ — **пошаговая инструкция для системного администратора** по созданию резервной копии и восстановлению системы с помощью подготовленных скриптов ReaR из данного репозитория.

Инструкция ориентирована на **практическое использование в production** и содержит **контрольные проверки на каждом этапе**.

---

## 0️⃣ Предварительные требования

Перед началом убедитесь, что:

- [ ] Система работает корректно
- [ ] Есть root-доступ
- [ ] Подготовлен отдельный USB-носитель
- [ ] Вы понимаете текущий disk layout (`lsblk`)

Проверка:
```bash
lsblk
mount
```

---

## 1️⃣ Подготовка среды и USB-носителя

### 1.1 Запуск автоматизации

```bash
sudo ./full-rear-setup.sh
```

Скрипт последовательно:
- анализирует систему;
- предлагает выбрать USB-устройство;
- выполняет проверки безопасности;
- форматирует USB (exFAT / FAT32);
- устанавливает и настраивает ReaR;
- генерирует rescue-образ.

❗ На этом этапе **ничего не форматируется без подтверждения администратора**.

---

## 2️⃣ Проверка после подготовки

### 2.1 Проверка конфигурации ReaR

```bash
cat /etc/rear/local.conf
```

Ожидаемо:
```text
BACKUP=NETFS
BACKUP_URL=file:///mnt/rear-usb/backups
OUTPUT=ISO
OUTPUT_URL=file:///mnt/rear-usb/output
```

---

### 2.2 Проверка layout

```bash
rear checklayout
```

Ожидаемо: отсутствие ошибок.

---

### 2.3 Генерация rescue-носителя (контроль)

```bash
rear mkrescue
```

Проверить наличие файлов:
```bash
ls /mnt/rear-usb/output
```

---

## 3️⃣ Создание резервной копии

### 3.1 Запуск backup

```bash
rear mkbackup
```

Контроль:
```bash
ls -lh /mnt/rear-usb/backups
```

Убедитесь, что размер backup соответствует ожиданиям.

---

## 4️⃣ Проверка готовности к Disaster Recovery

Перед аварией или тестом:

- [ ] Backup существует
- [ ] Rescue-носитель создан
- [ ] Проверен `rear checklayout`
- [ ] Чек-лист `RECOVERY-CHECKLIST.md` доступен

---

## 5️⃣ Восстановление системы (Disaster Recovery)

### 5.1 Загрузка в rescue-среду

- Загрузиться с rescue-ISO или USB
- Дождаться shell ReaR

---

### 5.2 Проверка USB и backup

```bash
mount | grep rear-usb
ls /mnt/rear-usb/backups
```

❗ Если USB не смонтирован — **не продолжать**.

---

### 5.3 Проверка layout

```bash
rear checklayout
```

Ознакомьтесь со всеми предупреждениями.

---

### 5.4 Recovery

```bash
rear recover
```

Подтверждайте действия **осознанно**.

---

## 6️⃣ Проверка после восстановления

После перезагрузки:

```bash
lsblk
df -h
journalctl -xb
```

Проверьте:
- загрузку системы;
- работу GRUB;
- состояние сервисов.

---

## ⚠️ Критические правила

- Никогда не запускайте `rear recover` без `rear checklayout`
- Никогда не выполняйте recovery вслепую
- Всегда используйте чек-лист администратора

---

## 📌 Рекомендуемые практики

- Тестируйте recovery в VM
- Храните минимум 2 копии backup
- Документируйте disk layout

---

## 🏁 Итог

Подготовленные скрипты позволяют:
- стандартизировать backup и recovery;
- снизить человеческий фактор;
- сохранить контроль за критическими этапами.

Disaster Recovery — это процесс, а не кнопка. Этот гайд помогает пройти его безопасно.

